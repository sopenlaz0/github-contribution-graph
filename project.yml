# project.yml
# XcodeGen project spec for generating the Xcode project.
# Run `xcodegen generate` to create GitHubContributions.xcodeproj

name: GitHubContributions
options:
  bundleIdPrefix: com.contributions
  deploymentTarget:
    macOS: "14.0"
  xcodeVersion: "16.2"
  minimumXcodeGenVersion: "2.35"

settings:
  base:
    SWIFT_VERSION: "5.9"
    MACOSX_DEPLOYMENT_TARGET: "14.0"

targets:
  GitHubContributions:
    type: application
    platform: macOS
    sources:
      - path: Sources
        type: group
    resources:
      - path: Resources/Assets.xcassets
    info:
      path: Resources/Info.plist
    entitlements:
      path: Resources/GitHubContributions.entitlements
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.contributions.GitHubContributions
        PRODUCT_NAME: GitHubContributions
        INFOPLIST_FILE: Resources/Info.plist
        # Xcode 16+ may merge entitlements while signing; allow it for local builds.
        CODE_SIGN_ALLOW_ENTITLEMENTS_MODIFICATION: YES
        COMBINE_HIDPI_IMAGES: true
